import 'package:flutter/material.dart';


class Wedding extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(
      title: Text('Events'),
    ),
    body: Center(child: Text('Data not available',style: TextStyle(fontSize: 40))),
  );

}
